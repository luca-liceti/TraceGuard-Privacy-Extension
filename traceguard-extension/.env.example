# TraceGuard Privacy Extension Environment Variables
# Copy this file to .env and configure as needed

# Currently, TraceGuard runs entirely locally and does not require external API keys.
# If external telemetry or cloud sync features are added in the future, configure them here.

# Example:
# VITE_API_BASE_URL=https://api.example.com
