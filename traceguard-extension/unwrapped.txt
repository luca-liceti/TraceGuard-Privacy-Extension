File: src/sidepanel/App.tsx
  - TraceGuard
File: src/hooks/use-mobile.tsx
  - window.innerWidth
File: src/components/theme-toggle.tsx
  - Dark
  - System
  - Toggle theme
  - Light
File: src/components/chart-area-interactive.tsx
  - Failed to load chart
  - Last 7 days
  - User Privacy Score
  - item.date
  - No data yet
  - Today
  - entry.timestamp
  - Last 30 days
  - Your privacy score history will appear here over time.
File: src/components/error-boundary.tsx
  - Try again
  - Something went wrong
File: src/components/section-cards.tsx
  - sum + (s.enrichedDetails?.fingerprinting?.summary.totalAttempts ?? 0), 0
  )
  const fpTrend = formatTrend(fpToday, fpYesterday)

  return (
  - sum + (s.enrichedDetails?.fingerprinting?.summary.totalAttempts ?? 0), 0
  )
  const totalFp = Object.values(siteCache as Record
  - = startOfToday)
  const cacheSitesToday = Object.values(siteCache as Record
  - piiYesterday ? "up" : piiToday
  - = startOfYesterday && ts
  - = startOfYesterday && log.timestamp
  - = trackersYesterday ? "up" : "down",
          value: trackersTrend,
          isPositive: trackersToday
  - = netYesterday.thirdParty ? "up" : "down",
          value: netTrend,
          isPositive: netToday.thirdParty
  - = fpYesterday ? "up" : "down",
          value: fpTrend,
          isPositive: fpToday
File: src/components/nav-documents.tsx
  - Share
  - Documents
  - More
  - Open
File: src/components/data-table.tsx
  - return (
  - Failed to load activity logs table
  - N/A
  - if (count === 0) return
  - getColumns(t), [t])

  // Build groups if not provided
  const groups = React.useMemo
  - []
  expandedDomains: Set
  - return
  - 1
        const s = group.summary

        return (
  - // ── Safety level ordering for worst-case aggregation ────────────────────────
const SAFETY_ORDER: Record
  - : s.fingerprintingAttempts === 0
                    ?
  - : visit.fingerprintingAttempts === 0
                      ?
File: src/components/radial-chart-score.tsx
  - = 0
  
  const timeText = timeRange === "1d" ? t("today") : timeRange === "7d" ? t("this week") : t("this month")

  return (
File: src/components/legacy-data-table.tsx
  - Showing total visitors for the last 6 months
  - Edit
  - Delete
  - Go to first page
  - Showing total visitors for the last 6 months. This is just
                  some random text to test the layout. It spans multiple lines
                  and should wrap around.
  - initialData)

  const [columnVisibility, setColumnVisibility] =
    React.useState
  - Target
  - Status
  - Jamik Tashpulatov
  - View
  - Reviewer
  - Favorite
  - Executive Summary
  - Header
  - Design
  - Emily Whalen
  - Make a copy
  - Eddie Lake
  - Go to previous page
  - In Progress
  - Outline
  - Capabilities
  - Type
  - Focus Documents
  - Limit
  - Narrative
  - Go to next page
  - Columns
  - Cover Page
  - Technical Approach
  - Add Section
  - Go to last page
  - (
    []
  )
  const [sorting, setSorting] = React.useState
  - Customize Columns
  - Done
  - Key Personnel
  - Rows per page
  - Past Performance
  - No results.
  - Table of Contents
  - Open menu
  - Not Started
  - Submit
File: src/components/team-switcher.tsx
  - Add team
  - Teams
File: src/components/ErrorBoundary.tsx
  - Try again
  - Report Issue on GitHub
  - Something went wrong
File: src/components/nav-footer.tsx
  - Ctrl ⇧ ,
File: src/components/ErrorBoundary.test.tsx
  - Safe child
File: src/components/traceguard/site-details-panel.tsx
  - Small files websites store on your device. Some are necessary, others track you for ads.
  - How this site says it handles your data, graded by community reviewers.
  - — set by this site only, no impact on your privacy score
  - All Points
  - Whether this site has been flagged as unsafe, malicious, or deceptive by security databases.
  - This site has a
  - Trackers
  - No cookies detected on this page.
  - Secure
  - received data about your visit
  - Bad Points
  - c.status === 'blocked')

                                // Group active by category
                                const grouped: Record
  - Classification Point
  - t.status === 'blocked')

                                // Group active by category
                                const grouped: Record
  - HttpOnly
  - Forms on this page that ask for sensitive info like passwords, emails, or credit cards.
  - Cookies
  - No personal data fields detected on this page.
  - No network activity recorded.
  - No cookies detected on this page
  - Connection Security
  - — follow you across the web to build an advertising profile
  - Techniques used to identify your device without cookies — harder to block and often invisible.
  - to the site itself — normal page behaviour
  - Couldn't check this site's connection security.
  - · active
  - Cookie · Domain
  - Result
  - ToS;DR
  - Other companies and servers this page shared data with during your visit.
  - Header
  - Risk
  - Personal Data Fields
  - Type
  - Fingerprinting
  - · missing
  - Domain · Organization
  - : isBlocked
                                                                        ?
  - return (
  - Rating
  - This site has been flagged as
  - !h.present)
                                return (
  - Good Points
  - on this page — they may be collecting data about your visit
  - suspicious or unsafe
  - — no safety warnings found in any database
  - clean reputation
  - before they could load
  - Status
  - No safety warnings found for this site.
  - No special flags
  - Third-Party Connections
  - : f.risk === 'medium'
                                                                    ?
  - Category
  - 0

    return (
  - 0 && types.length === 0 && (
  - Privacy Policy
  - (ORDER.indexOf(a) === -1 ? 99 : ORDER.indexOf(a)) - (ORDER.indexOf(b) === -1 ? 99 : ORDER.indexOf(b))
                                )

                                return (
  - No fingerprinting detected — your device identity is safe here.
  - Companies that follow your activity across websites to build a profile about you.
  - detected — this page collects personal information
  - Documents
  - This page asks for your
  - No trackers found — this is good!
  - No privacy policy information available.
  - Security Check
  - Third Party
  - Technique · Script Domain
  - Reputation
  - Protections the site uses to keep your connection safe from eavesdropping and tampering.
  - Blockers
  - Organization
  - contacted (CDNs, fonts, APIs, etc.)
  - in one or more security databases
  - — track how you use this site and share data with other services
  - Neutral
  - Grade:
File: src/components/traceguard/overview-tile.tsx
  - out of 100
  - View details
  - Your Privacy Score
  - View full report
File: src/components/traceguard/search-command.tsx
  - (null)
  const [selectedDomain, setSelectedDomain] = React.useState
File: src/components/traceguard/settings-modal.tsx
  - (settings?.whitelist || [])
    const [blacklist, setBlacklist] = useState
  - 3 days
  - 7 days
  - 30 days
  - 14 days
  - 1 day
File: src/components/traceguard/notifications.tsx
  - case 'info':
        default:
            return
  - Clear all notifications
  - Mark all read
  - No notifications yet
  - Notifications
  - You'll see alerts for risky sites and PII detection here
  - case 'warning':
            return
File: src/components/traceguard/auth-provider.tsx
  - Secure Your Vault
  - = 10,
                      /[A-Z]/.test(password),
                      /[0-9]/.test(password),
                      /[^A-Za-z0-9]/.test(password),
                    ];
                    const strength = checks.filter(Boolean).length;
                    const colors = ['bg-red-500', 'bg-orange-500', 'bg-yellow-500', 'bg-emerald-500'];
                    const labels = ['Weak', 'Fair', 'Good', 'Strong'];
                    return (
  - Vault Locked
  - Create a Master Password to encrypt your privacy logs.
  - Promise
  - Enter your Master Password to access your privacy logs.
  - &larr; Back to Setup
  - What should we call you?
  - Master Password
  - Privacy Policy
  - Before continuing, please review our
  - Confirm Password
File: src/components/traceguard/pages/help.tsx
  - 0 || !searchQuery) && (
  - item.problem.toLowerCase().includes(searchQuery.toLowerCase()) || 
    item.solution.toLowerCase().includes(searchQuery.toLowerCase())
  )

  return (
File: src/components/traceguard/pages/privacy-score.tsx
  - ) : trend
  - entry.ups))
        : state.ups

    const scoreLevel = getStatusConfig(state.ups)

    return (
File: src/components/traceguard/pages/rankings.tsx
  - r.events === 0) ? (
  - = startOfYesterday && l.timestamp
  - d.events === 0) ? (
File: src/components/sidepanel/site-details.tsx
  - = 60) return
  - ;
    return
  - ))
ScrollArea.displayName = ScrollAreaPrimitive.Root.displayName

const ScrollBar = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - * 
 * With icon and color:
 *
  - * 
 * With trend indicator:
 *
  - * 
 * With subtitle and link:
 *
  - Next
  - )
Pagination.displayName = "Pagination"

const PaginationContent = React.forwardRef
  - More pages
  - &
  React.ComponentProps
  - ))
PaginationContent.displayName = "PaginationContent"

const PaginationItem = React.forwardRef
  - Previous
  - ))
TabsTrigger.displayName = TabsPrimitive.Trigger.displayName

const TabsContent = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
TabsList.displayName = TabsPrimitive.List.displayName

const TabsTrigger = React.forwardRef
  - ))
BreadcrumbItem.displayName = "BreadcrumbItem"

const BreadcrumbLink = React.forwardRef
  - ))
BreadcrumbList.displayName = "BreadcrumbList"

const BreadcrumbItem = React.forwardRef
  - More
  - )
Breadcrumb.displayName = "Breadcrumb"

const BreadcrumbList = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - &
    VariantProps
  - ,
  React.ComponentPropsWithoutRef
  - ))
CardTitle.displayName = "CardTitle"

const CardDescription = React.forwardRef
  - ))
CardDescription.displayName = "CardDescription"

const CardContent = React.forwardRef
  - ))
Card.displayName = "Card"

const CardHeader = React.forwardRef
  - ))
CardContent.displayName = "CardContent"

const CardFooter = React.forwardRef
  - ))
CardHeader.displayName = "CardHeader"

const CardTitle = React.forwardRef
  - &
  VariantProps
  - & VariantProps
  - &
    React.ComponentProps
  - ) : (
                        !hideIndicator && (
  - &
    Pick
  - ,
  React.ComponentPropsWithoutRef
  - ))
SelectScrollDownButton.displayName =
  SelectPrimitive.ScrollDownButton.displayName

const SelectContent = React.forwardRef
  - ))
SelectLabel.displayName = SelectPrimitive.Label.displayName

const SelectItem = React.forwardRef
  - ))
SelectScrollUpButton.displayName = SelectPrimitive.ScrollUpButton.displayName

const SelectScrollDownButton = React.forwardRef
  - ))
SelectContent.displayName = SelectPrimitive.Content.displayName

const SelectLabel = React.forwardRef
  - ))
SelectTrigger.displayName = SelectPrimitive.Trigger.displayName

const SelectScrollUpButton = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
SelectItem.displayName = SelectPrimitive.Item.displayName

const SelectSeparator = React.forwardRef
  - ))
ToastClose.displayName = ToastPrimitives.Close.displayName

const ToastTitle = React.forwardRef
  - &
    VariantProps
  - ,
  React.ComponentPropsWithoutRef
  - ))
ToastDescription.displayName = ToastPrimitives.Description.displayName

type ToastProps = React.ComponentPropsWithoutRef
  - ))
ToastTitle.displayName = ToastPrimitives.Title.displayName

const ToastDescription = React.forwardRef
  - type ToastActionElement = React.ReactElement
  - ))
ToastAction.displayName = ToastPrimitives.Action.displayName

const ToastClose = React.forwardRef
  - ))

CommandInput.displayName = CommandPrimitive.Input.displayName

const CommandList = React.forwardRef
  - ))

CommandEmpty.displayName = CommandPrimitive.Empty.displayName

const CommandGroup = React.forwardRef
  - ))

CommandGroup.displayName = CommandPrimitive.Group.displayName

const CommandSeparator = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))

CommandList.displayName = CommandPrimitive.List.displayName

const CommandEmpty = React.forwardRef
  - ))
CommandSeparator.displayName = CommandPrimitive.Separator.displayName

const CommandItem = React.forwardRef
  - ,
    VariantProps
  - ,
  React.ComponentPropsWithoutRef
  - Previous slide
  - [0]
  api: ReturnType
  - Next slide
  - ))
TableBody.displayName = "TableBody"

const TableFooter = React.forwardRef
  - ))
TableHeader.displayName = "TableHeader"

const TableBody = React.forwardRef
  - ))
TableHead.displayName = "TableHead"

const TableCell = React.forwardRef
  - ))
TableFooter.displayName = "TableFooter"

const TableRow = React.forwardRef
  - ))
TableRow.displayName = "TableRow"

const TableHead = React.forwardRef
  - ))
Table.displayName = "Table"

const TableHeader = React.forwardRef
  - ))
TableCell.displayName = "TableCell"

const TableCaption = React.forwardRef
  - ))
SidebarMenu.displayName = "SidebarMenu"

const SidebarMenuItem = React.forwardRef
  - )
SidebarMenuSubItem.displayName = "SidebarMenuSubItem"

const SidebarMenuSubButton = React.forwardRef
  - Toggle Sidebar
  - ))
SidebarMenuSub.displayName = "SidebarMenuSub"

const SidebarMenuSubItem = React.forwardRef
  - Displays the mobile sidebar.
  - ))
SidebarGroupContent.displayName = "SidebarGroupContent"

const SidebarMenu = React.forwardRef
  - ))
SidebarMenuBadge.displayName = "SidebarMenuBadge"

const SidebarMenuSkeleton = React.forwardRef
  - ,
  React.ComponentProps
  - Sidebar
  - = FieldPath
  - ,
  React.ComponentPropsWithoutRef
  - (null)

const FormField =
  - (null)

const FormItem = React.forwardRef
  - & VariantProps
  - ,
  React.ComponentPropsWithoutRef
  - ))
Alert.displayName = "Alert"

const AlertTitle = React.forwardRef
  - ))
AlertTitle.displayName = "AlertTitle"

const AlertDescription = React.forwardRef
  - & VariantProps
  - ))
AccordionTrigger.displayName = AccordionPrimitive.Trigger.displayName

const AccordionContent = React.forwardRef
  - ))
AccordionItem.displayName = "AccordionItem"

const AccordionTrigger = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
InputOTPGroup.displayName = "InputOTPGroup"

const InputOTPSlot = React.forwardRef
  - ))
InputOTP.displayName = "InputOTP"

const InputOTPGroup = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - &
    VariantProps
  - ,
  React.ComponentPropsWithoutRef
  - ))

ToggleGroup.displayName = ToggleGroupPrimitive.Root.displayName

const ToggleGroupItem = React.forwardRef
  - ,
    VariantProps
  - )
Drawer.displayName = "Drawer"

const DrawerTrigger = DrawerPrimitive.Trigger

const DrawerPortal = DrawerPrimitive.Portal

const DrawerClose = DrawerPrimitive.Close

const DrawerOverlay = React.forwardRef
  - ))
DrawerTitle.displayName = DrawerPrimitive.Title.displayName

const DrawerDescription = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
DrawerOverlay.displayName = DrawerPrimitive.Overlay.displayName

const DrawerContent = React.forwardRef
  - )
DrawerFooter.displayName = "DrawerFooter"

const DrawerTitle = React.forwardRef
  - &
  VariantProps
  - & VariantProps
  - ))
DropdownMenuLabel.displayName = DropdownMenuPrimitive.Label.displayName

const DropdownMenuSeparator = React.forwardRef
  - ))
DropdownMenuContent.displayName = DropdownMenuPrimitive.Content.displayName

const DropdownMenuItem = React.forwardRef
  - ))
DropdownMenuSubTrigger.displayName =
  DropdownMenuPrimitive.SubTrigger.displayName

const DropdownMenuSubContent = React.forwardRef
  - ))
DropdownMenuItem.displayName = DropdownMenuPrimitive.Item.displayName

const DropdownMenuCheckboxItem = React.forwardRef
  - ))
DropdownMenuCheckboxItem.displayName =
  DropdownMenuPrimitive.CheckboxItem.displayName

const DropdownMenuRadioItem = React.forwardRef
  - ))
DropdownMenuRadioItem.displayName = DropdownMenuPrimitive.RadioItem.displayName

const DropdownMenuLabel = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
DropdownMenuSubContent.displayName =
  DropdownMenuPrimitive.SubContent.displayName

const DropdownMenuContent = React.forwardRef
  - &
    VariantProps
  - ,
  React.ComponentPropsWithoutRef
  - ))
AlertDialogAction.displayName = AlertDialogPrimitive.Action.displayName

const AlertDialogCancel = React.forwardRef
  - ))
AlertDialogTitle.displayName = AlertDialogPrimitive.Title.displayName

const AlertDialogDescription = React.forwardRef
  - )
AlertDialogFooter.displayName = "AlertDialogFooter"

const AlertDialogTitle = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
AlertDialogOverlay.displayName = AlertDialogPrimitive.Overlay.displayName

const AlertDialogContent = React.forwardRef
  - ))
AlertDialogDescription.displayName =
  AlertDialogPrimitive.Description.displayName

const AlertDialogAction = React.forwardRef
  - ))
AvatarImage.displayName = AvatarPrimitive.Image.displayName

const AvatarFallback = React.forwardRef
  - ))
Avatar.displayName = AvatarPrimitive.Root.displayName

const AvatarImage = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - Close
  - )
DialogFooter.displayName = "DialogFooter"

const DialogTitle = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
  - ))
DialogTitle.displayName = DialogPrimitive.Title.displayName

const DialogDescription = React.forwardRef
  - ))
DialogOverlay.displayName = DialogPrimitive.Overlay.displayName

const DialogContent = React.forwardRef
  - error?.message &&
  - & VariantProps
  - & VariantProps
  - )
SheetFooter.displayName = "SheetFooter"

const SheetTitle = React.forwardRef
  - Close
  - ,
  React.ComponentPropsWithoutRef
  - ,
    VariantProps
  - ))
SheetTitle.displayName = SheetPrimitive.Title.displayName

const SheetDescription = React.forwardRef
  - ,
  React.ComponentPropsWithoutRef
